# node_modules: 의존성 라이브러리 (업로드하지 않음)
node_modules/

# Vite 빌드 결과물
dist/

# 환경 변수 파일
.env
.env.*.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제나 에디터에서 생기는 불필요한 파일
.DS_Store
Thumbs.db

# IDE 설정 파일들 (선택적으로 포함)
.vscode/
.idea/

# eslint 캐시
.eslintcache

# coverage 폴더 (테스트 커버리지)
coverage/

# local 개발 서버 캐시 (vite 사용 시)
.cache/